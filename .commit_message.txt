💄 한글 탭 디자인을 다른 탭(카드형)과 통일 + 변환 시 파일 접근 권한 팝업 자동 수락

- 디자인: ttk.LabelFrame → _create_section(카드형) + Card.TFrame + Secondary/Accent.TButton으로 PDF 탭과 동일하게. 한글 탭만 튀던 문제 해결.
- 권한: _hwp_suppress_security_prompt 추가(_get_hwp_app에서 호출) → RegisterModule("FilePathCheckDLL","FilePathCheckerModule")로 파일 접근 보안 승인 창 자동 수락. 모듈 없으면 무시.
- APP_BUILD_ID=2026-06-11-hwp-ui.
